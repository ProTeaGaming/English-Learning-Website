# Yee Universe — Build Plan & Log

> Read this file before making changes to this project. After finishing a
> unit of work, append a new entry to the **Build Log** below so future work
> can pick up the context quickly.

## Project shape

- React 19 + Vite + Tailwind + React Router + GSAP + lucide-react.
- `src/pages/` — top-level pages (`PrivacyPolicy`, `Terms`, etc.).
- `src/pages/characters/` — character-related pages (`Characters`,
  `Protagonists`, `SideCharacters`, `Teams`, `CharacterProfile`, shared
  helpers in `shared.jsx`).
- `src/data/characters.js` — character data.

## Build Log

> ⚠️ Note: a previous `COPY_PLAN.md` existed in this directory but was
> accidentally deleted during cleanup of unrelated test files in another
> project, and its contents could not be recovered. This file was recreated
> as a fresh template on 2026-06-13. Add a dated entry below each time work
> is done on this project.

### 2026-06-13 — File recreated

This file was recreated empty after accidental deletion. No build entries
yet — add new dated sections above this note as work happens.
